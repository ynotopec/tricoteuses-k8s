apiVersion: apps/v1
kind: Deployment
metadata:
  name: moulineuse
  namespace: demo1
  labels:
    app: tricoteuses-moulineuse
    app.kubernetes.io/name: moulineuse
    app.kubernetes.io/component: mcp
    app.kubernetes.io/part-of: tricoteuses
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tricoteuses-moulineuse
  template:
    metadata:
      labels:
        app: tricoteuses-moulineuse
        app.kubernetes.io/name: moulineuse
        app.kubernetes.io/component: mcp
    spec:
      containers:
      - name: moulineuse
        image: registry.demo1.ailab.infocepo.com/tricoteuses/moulineuse:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: PORT
          value: "8000"
        - name: NODE_ENV
          value: production
        - name: MCP_HTTP_HOST
          value: "0.0.0.0"
        - name: MCP_ALLOWED_HOSTS
          value: "localhost,moulineuse.demo1.ailab.infocepo.com"
        - name: MCP_ALLOWED_ORIGINS
          value: "https://exploratrice.demo1.ailab.infocepo.com,https://moulineuse.demo1.ailab.infocepo.com"
        - name: MCP_AUTH_REQUIRED
          value: "true"
        - name: MCP_AUTH_ISSUER
          value: "https://auth.code4code.eu/realms/tricoteuses"
        - name: MCP_AUTH_RESOURCE
          value: "https://moulineuse.demo1.ailab.infocepo.com/mcp"
        - name: MCP_AUTH_AUDIENCE
          value: "https://moulineuse.demo1.ailab.infocepo.com/mcp"
        - name: MCP_AUTH_AUTHORIZATION_ENDPOINT
          value: "https://auth.code4code.eu/realms/tricoteuses/protocol/openid-connect/auth"
        - name: MCP_AUTH_TOKEN_ENDPOINT
          value: "https://auth.code4code.eu/realms/tricoteuses/protocol/openid-connect/token"
        - name: MCP_AUTH_JWKS_URL
          value: "https://auth.code4code.eu/realms/tricoteuses/protocol/openid-connect/certs"
        envFrom:
        - configMapRef:
            name: tricoteuses-db-config
        - secretRef:
            name: tricoteuses-secrets
        livenessProbe:
          exec:
            command:
            - node
            - -e
            - "require('http').get('http://localhost:8000/health', (r) => { process.exit(r.statusCode === 200 ? 0 : 1); }).on('error', () => process.exit(1));"
          initialDelaySeconds: 5
          periodSeconds: 30
          failureThreshold: 3
          timeoutSeconds: 5
          successThreshold: 1
        readinessProbe:
          exec:
            command:
            - node
            - -e
            - "require('http').get('http://localhost:8000/health', (r) => { process.exit(r.statusCode === 200 ? 0 : 1); }).on('error', () => process.exit(1));"
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
          timeoutSeconds: 5
          successThreshold: 1
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
